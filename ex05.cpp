#include <iostream>

using namespace std;

int main (int argc, char** argv) 
{
	float v1, v2, v3, v4, m;
	 
	 cout << "Digite as 4 notas separadas por espaço : " ;
	 cin >> v1 >> v2 >> v3 >> v4;
	 m = (v1 + v2 + v3 + v4) / 4;
	 cout << " A media das notas e: " << m << endl;
	
	 
	 return 0;
	 
	 
	
	  
	
	
}
